export type TEvent = {
    id: number
    title: string
    category: string
    description: string
    duration: string
    cities: Array<string>
}

export type TCity = {
    id: number
    name: string
}